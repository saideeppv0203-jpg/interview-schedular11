{
  "name": "interview-scheduler",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "install-all": "npm install --prefix server && npm install --prefix client --include=dev",
    "dev": "concurrently -n SERVER,CLIENT -c blue,green \"npm run start --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm install --prefix server && npm install --prefix client --include=dev && npm run build --prefix client",
    "start": "node server/index.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
